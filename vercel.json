{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/smart-ai run build && mv artifacts/smart-ai/dist dist",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    }
  ]
}
